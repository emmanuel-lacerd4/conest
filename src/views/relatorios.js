/**
 * Processo de renderização da tela de Relatórios
 * relatorios.js
 */

// Função para gerar o relatório de clientes
function gerarRelatorioClientes() {
    window.api.gerarRelatorioClientes()
}

// Função para gerar o relatório de fornecedores
function gerarRelatorioFornecedores() {
    window.api.gerarRelatorioFornecedores()
}

// Função para gerar o relatório de produtos
function gerarRelatorioProdutos() {
    window.api.gerarRelatorioProdutos()
}